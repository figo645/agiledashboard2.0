当前HTML中，5个chart的id如下
id="sprintTable"
id="completionChart"
id="changeChart"
id="testingChart"
id="bugChart"

将这5个id对应的函数内的静态数据，变成从后端api获取数据
后端API对应的也成为五个，分别是
/api/sprintTable
/api/completionChart
/api/changeChart
/api/testingChart
/api/bugChart

改为这个形态好，我需要确保画面layout和内容和之前一样，但是数据从后端获取
请从后台代码提供测试数据，并确保数据格式正确
index.html的5个chart在改动后应该与静态的保持一致，数据从后端获取