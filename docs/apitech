当前HTML中，5个chart的id如下
id="sprintTable"
id="completionChart"
id="changeChart"
id="testingChart"
id="bugChart"

将这5个id对应的函数内的静态数据，变成从后端api获取数据
后端API对应的也成为五个，分别是
/api/sprintTable
/api/completionChart
/api/changeChart
/api/testingChart
/api/bugChart

改为这个形态好，我需要确保画面layout和内容和之前一样，但是数据从后端获取
其对应的json文件也请一并生成，并放在src/main/resources/static/data目录下

这些api后台函数，仅作为本次测试用，之后我会用真实的api地址来替换